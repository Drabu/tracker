/// Stub implementation for web platform
Future<void> initDeepLinks({required Function(Uri) onLink}) async {
  // Web platform doesn't use uni_links
  // OAuth callback would be handled via web redirect
}
